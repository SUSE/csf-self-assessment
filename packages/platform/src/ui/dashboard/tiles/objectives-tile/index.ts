// The Objectives tile: where does weakness coincide with leverage?

// `objectives-ring` is the drawing; its geometry comes from ui/wheel, shared
// with the other wheels, and the Report draws it too (report.md §3.3).
export { default as ObjectivesTile } from './objectives-tile.svelte';
export { default as ObjectivesRing } from './objectives-ring.svelte';
