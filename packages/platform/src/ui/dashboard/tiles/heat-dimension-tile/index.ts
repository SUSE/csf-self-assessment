// Weakness by dimension: which part of the estate is weak?
//
// One axis of the shared heat grid (../heat-tile.svelte) — the four heat tiles
// are one component on four axes, so the grid stays in the tiles root.
export { default as HeatDimensionTile } from './heat-dimension-tile.svelte';
