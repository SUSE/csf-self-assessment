// The Estate wheel tile: where are we weakest, in one frame?

// `estate-wheel-figure` is the drawing shared by this tile and the Report —
// the geometry and marks it shares with the other wheels live in ui/wheel.
export { default as EstateWheelTile } from './estate-wheel-tile.svelte';
export { default as EstateWheelFigure } from './estate-wheel-figure.svelte';
